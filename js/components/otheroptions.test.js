import React from 'react';
import {shallow, mount, render} from 'enzyme';

import OtherOptions from './otheroptions';

describe('<OtherOptions />', () => {
    it('Renders without crashing', () => {
        shallow(<OtherOptions />);
                });
    });
